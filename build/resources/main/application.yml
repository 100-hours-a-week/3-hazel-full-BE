spring:
  datasource:
    url: jdbc:mysql://localhost:3306/up_date?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: hazel
    password: hazel123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: info